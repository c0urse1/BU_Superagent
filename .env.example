# Embeddings (infra/core) — E5 default
# Primary flat keys (new default)
EMBEDDING_PROVIDER=huggingface
EMBEDDING_MODEL_NAME=intfloat/multilingual-e5-large-instruct
EMBEDDING_DEVICE=auto
NORMALIZE_EMBEDDINGS=true
EMBEDDINGS__BATCH_SIZE=64

# Legacy/alternate keys (kept for compatibility)
EMBEDDINGS__PROVIDER=huggingface
EMBEDDINGS__MODEL_NAME=intfloat/multilingual-e5-large-instruct
EMBEDDINGS__DEVICE=auto

# E5 prefixing controls (defaults shown)
E5_ENABLE_PREFIX=true
E5_QUERY_INSTRUCTION=Instruct: Given a web search query, retrieve relevant passages that answer the query
E5_QUERY_PREFIX=Query: 
E5_PASSAGE_PREFIX=Passage: 

# CLI (bu_kb) overrides (KB_ prefix)
KB_EMBED_MODEL=intfloat/multilingual-e5-large-instruct
KB_COLLECTION_NAME=bu_knowledge
# Default CLI persist dir remains .vector_store/chroma unless E5 model is detected
# KB_PERSIST_DIR=.vector_store/chroma
# KB_SOURCE_DIR=data/pdfs

# OpenAI (opt-in)
# Use only if your data processing and cross-border transfer are allowed and documented.
# Keep PII to a minimum and obtain user consent where required.
# These are explicit requirements in security & compliance principles:
# data minimization, legal basis, transparency, human-in-the-loop.
#
# EMBEDDINGS__PROVIDER=openai
# EMBEDDINGS__MODEL_NAME=text-embedding-3-small
# OPENAI_API_KEY=sk-...
# Optional for Azure/OpenAI-compatible endpoints
# EMBEDDINGS__OPENAI_BASE_URL=https://<your-azure-endpoint>
